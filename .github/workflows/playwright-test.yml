name: Run Playwright Tests

on:
  push:

jobs:
  lancement-appli-et-test:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.32.0-focal

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: 17

      - name: Préparation Gradle
        uses: gradle/gradle-build-action@v2
      - name: Lancement de l'application
        continue-on-error: true
        run: |
          ./gradlew bootRun --args='--spring.config.location=classpath:/application-ci.properties' &                    
          set +e
          curl --retry 10 --retry-connrefused --retry-delay 5 -o /dev/null --fail "http://localhost:8080/formations"
#          sleep 10s &&
#          tail -n 100  log/spring.log || true
#          sleep 10s &&
#          tail -n 100  log/spring.log || true
#          sleep 10s &&
#          tail -n 100  log/spring.log || true
#          sleep 10s &&
#          tail -n 100  log/spring.log || true
#          curl http://localhost:8080/formations -I


#      - name: Attente de la disponibilité de l'application
#        uses: cygnetdigital/wait_for_response@v2.0.0
#        with:
#          url: 'http://localhost:8080/formations'

      - name: Préparation Gradle
        uses: gradle/gradle-build-action@v2
      - name: Run tests
        run: |
          cd playwright_java
          ls
          pwd
          ./gradlew test
